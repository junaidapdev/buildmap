export function DashboardPlaceholder() {
  return (
    <section>
      {/* TODO(chunk-07): replace with the real dashboard project list. */}
      <h1 className="text-2xl font-semibold tracking-tight">Dashboard</h1>
      <p className="mt-2 text-muted-foreground">Project list lands here in Chunk 07.</p>
    </section>
  );
}
