export function ProjectModePlaceholder() {
  return (
    <section>
      {/* TODO(chunk-11): replace this routing stub with the real project workspace layout. */}
      <h1 className="text-2xl font-semibold tracking-tight">Project workspace</h1>
      <p className="mt-2 text-muted-foreground">
        Project pages become available in their owning feature chunks.
      </p>
    </section>
  );
}
